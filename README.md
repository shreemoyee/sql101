# sql101
sqllite database and android project
A simple android project that hosts an SQLlite database and insets, deletes, updates information to it along with 
displaying the database on the home page. 
Uses floating action button.
